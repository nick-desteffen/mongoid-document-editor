<div class="section header">
  <div class="container clear">
    <h1 class='logo'><%= @klass.name.pluralize %></h1>
  </div>
</div>

<table>
  <thead>
      <tr>
        <% Mongoid::DocumentEditor.index_for(@klass).each_pair do |header, config| %>
          <th><%= header.to_s.humanize %></th>
        <% end %>
          <th>Actions</th>
      </tr>
  </thead>
  <tbody>
    <% @documents.each do |document| %>
      <tr>
        <% Mongoid::DocumentEditor.index_for(@klass).each_pair do |header, config| %>
          <td><%= document.send(header) %></td>
        <% end %>
        <td>
          <%= link_to "View", document_path(@klass, document) %> |
          <%= link_to "Edit", edit_document_path(@klass, document) %> |
          <%= link_to "Destroy", destroy_document_path(@klass, document), method: :delete, data: {confirm: "Are you sure you want to destroy this record?"} %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
